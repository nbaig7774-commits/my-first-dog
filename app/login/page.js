"use client"; import {useState} from "react"; import Link from "next/link"; import {createClient} from "../../lib/supabase"; export default function Login(){const sb=createClient();const[email,setEmail]=useState("");const[pw,setPw]=useState("");const[mode,setMode]=useState("login");const[msg,setMsg]=useState("");async function go(e){e.preventDefault();const r=mode==="login"?await sb.auth.signInWithPassword({email,password:pw}):await sb.auth.signUp({email,password:pw});if(r.error)setMsg(r.error.message);else if(mode==="login")location.href="/dashboard";else setMsg("Account created. Check your email if confirmation is enabled.")}return <main className="container"><form className="card form" onSubmit={go}><Link href="/">← My First Dog</Link><h1>{mode==="login"?"Welcome back":"Create account"}</h1><input className="input" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="input" type="password" placeholder="Password" value={pw} onChange={e=>setPw(e.target.value)} minLength="6" required/><button className="btn primary" style={{width:"100%"}}>{mode==="login"?"Log in":"Create account"}</button><p className="muted">{msg}</p><button type="button" className="btn" onClick={()=>setMode(mode==="login"?"signup":"login")}>{mode==="login"?"Create account":"Back to login"}</button></form></main>}